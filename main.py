import sys
import mymod
print(sys.path)
# Task 2
# sys.path.insert()
# Task 3
mymod.count_chars('mymod.py')
# PYTHONPATH doesn't need to include the directory because it was created in the pythonProject2 folder which has been
# already added to the sys.path
